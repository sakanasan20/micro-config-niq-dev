server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///${user.home}/niq/eclipse/eclipse-workspace/micro-config-niq-dev/config-files